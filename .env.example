# Copy this file to `.env` and fill values before running Docker Compose
# MongoDB connection (used by server/db.ts)
MONGO_URI=mongodb://mongo:27017/hospital

# JWT secret for auth
JWT_SECRET=replace-with-a-secure-secret

# VAPID keys generated via `npx web-push generate-vapid-keys`
VAPID_PUBLIC_KEY=your_public_vapid_key_here
VAPID_PRIVATE_KEY=your_private_vapid_key_here
VAPID_EMAIL=mailto:you@example.com

# Optional: points and other env settings
POINTS_PER_MED_TAKEN=10
